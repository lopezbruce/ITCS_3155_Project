<head>
    <title>Breakout!</title>
    <meta charset = "utf-8" />
    
    <style>
        * {padding: 0; margin: 0; }
    </style>
    <%= stylesheet_link_tag    "application", :media => "all" %>

   <!-- <link rel="stylesheet" type="text/css" href="/app/assets/stylesheets/welcome.css"> -->
</head>

<body>
    <div class = "navbar">
          <div>
          <div style="float:left;">
               <%= button_to "🏠", '/back', :method => 'get', :id => 'back'%>
            </div>
            <div >
                <div style="float:right;">
                    <% if logged_in? %>
                   <p style="padding:10px;"> hello, <%=current_user.username%></p>
                    <% else %>
                    
                    <% end %>
                </div>
                <h1 id="inner">Breakout!</h1>
          </div>
        </div>
        </div>
     
        
      <div>
        <br/><br/><br/>
        <button id ="startButton" class = "btn" type = "button">START</button><br/>
          <div>
                <canvas id= "myCanvas" width = "601px" height = "400px"></canvas>
                
                <%= javascript_include_tag 'breakout1.js' %>
            </div>
          </div>
          <br/>
          <div id="inner">
        
          <% if logged_in? %>
          <p>User: <%= link_to current_user.email, current_user%></p>
          <%= current_user.username%>
          <% else %>
          <p>Score will not submit unless you are logged in!</p>
          <%= button_to 'Sign in', '/login', :method => 'get', :id => 'signin' %>
        <% end %>
        
     
        </div>
        
        <div class = bottombar style="float: right">
    <%= link_to 'About Us', :method => 'get', :id => 'about' %>
    <%= link_to 'Privacy Policy', :method => 'get', :id => 'privacyPolicy' %>
    <% if logged_in? %>
           <%= link_to "Profile", current_user %>
          <%= link_to "Log out", logout_path, method: :delete %>
        <% else %>
          <%= link_to "Log in", login_path %>
        <% end %>
  </div>
    
</body>
